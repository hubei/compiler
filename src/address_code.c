#include "address_code.h"
#include "types.h"

string IRtoString() {
	char* result = malloc(sizeof("Test"));
	strcpy(result, "Test");
	return result;
}

irCode* getIRCode() {
	irCode* result;
	return result;
}
