/*
 * address_code.h
 *
 *  Created on: Mar 27, 2012
 *      Author: SebastianN
 */
#ifndef __ADDRESSCODE_HEADER__
#define __ADDRESSCODE_HEADER__

#include "types.h"
#include "symboltable.h"

void emit(string); //<*AUTOGENERATED LINE*> STATEMENT
void backpatch(int, int); //Zeile,

void printIRCode(FILE*, irCode_t*);
char* getConstOrId(irCode_arg_t*);
char* opToStr(operations_t);
/**
 * @brief Converts an IRCode into a string
 */
string IRtoString();
char* lineToString(char*, irCode_t*);
void printParam(irCode_t*);
/**
 * @brief Real conversion from expressions to IRCode
 */
expr_t* createIRCodeFromExpr(symbol_t*, expr_t*, operations_t, expr_t*);
irCode_t* getIRCode();

#endif
